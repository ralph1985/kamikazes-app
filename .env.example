# Server-only Neon connection string. Never expose this with NEXT_PUBLIC_.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional local override for the public app URL.
NEXT_PUBLIC_APP_URL=http://localhost:3000
